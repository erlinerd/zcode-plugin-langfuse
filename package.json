{
  "name": "zcode-langfuse-observability",
  "version": "0.1.0",
  "private": true,
  "description": "A fail-open Langfuse observability plugin for ZCode.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc --noEmit && node scripts/build.mjs",
    "clean": "node scripts/clean.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "validate": "node scripts/validate.mjs",
    "validate:artifact": "node scripts/validate.mjs --artifact",
    "package:plugin": "npm run build && npm run validate && node scripts/package-plugin.mjs && npm run validate:artifact",
    "check": "npm run typecheck && npm test && npm run validate",
    "prepare": "npm run build"
  },
  "dependencies": {
    "langfuse": "^3.38.20"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
